global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  # - "first_rules.yml"
  # - "second_rules.yml"

scrape_configs:
  # Job Sight Application
  - job_name: 'job-sight'
    static_configs:
      - targets: ['job-sight-app.ondigitalocean.app:443']
    metrics_path: '/metrics'
    scheme: 'https'
    scrape_interval: 30s
    
    # Add basic auth if needed
    # basic_auth:
    #   username: 'your-username'
    #   password: 'your-password'
    
    # TLS configuration for HTTPS
    tls_config:
      insecure_skip_verify: false
    
    # Relabeling for better metric organization
    relabel_configs:
      - source_labels: [__address__]
        target_label: instance
      - source_labels: [__meta_kubernetes_pod_name]
        target_label: pod
      - source_labels: [__meta_kubernetes_namespace]
        target_label: namespace

  # Self-monitoring
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

# Alerting rules (optional - can be configured in Grafana instead)
# rule_files:
#   - "job_sight_alerts.yml"
